# مستندات آموزشی و فنی پروژه تحلیل هوشمند نظرات (RAG-Sentiment)

این راهنما برای یادگیری عمیق و ارائه ویدئویی/کلاسی شما تهیه شده است. در اینجا خط به خط کدها و منطق پروژه را بررسی می‌کنیم.

---

## ۱. ساختار کلی پروژه (High-Level Architecture)
این پروژه از معماری **RAG (Retrieval-Augmented Generation)** استفاده می‌کند. برخلاف مدل‌های ساده که فقط یک عدد (مثبت/منفی) می‌دهند، این سیستم:
1.  **احساس** را تشخیص می‌دهد.
2.  در دیتابیس **جستجو** می‌کند تا نظرات مشابه را پیدا کند.
3.  یک **دلیل منطقی** تولید می‌کند که چرا این نظر چنین احساسی دارد.

---

## ۲. تحلیل خط به خط فایل `processor.py` (قلب پروژه)

### بخش اول: وارد کردن کتابخانه‌ها
- `transformers`: برای استفاده از مدل‌های BERT و GPT-2.
- `sentence-transformers`: برای تبدیل جملات به بردار (Embedding).
- `faiss`: کتابخانه فیس‌بوک برای جستجوی فوق سریع در بین هزاران بردار.

### بخش دوم: کلاس `SentimentRAG`
این کلاس تمام منطق هوش مصنوعی را در خود جای داده است.

#### متد `__init__`:
- **Sentiment Model:** مدل `nlptown/bert` را لود می‌کند. این مدل چندزبانه است و نظرات را از ۱ تا ۵ ستاره طبقه‌بندی می‌کند.
- **Embedding Model:** مدل `MiniLM-L12` را لود می‌کند. این مدل متن فارسی را به یک بردار ۳۸۴ بعدی تبدیل می‌کند.
- **GPT2 Model:** مدل `gpt2-fa-comment` که توسط هوشواره برای زبان فارسی بهینه شده، برای بخش "توضیح‌دهی" استفاده می‌شود.
- **FAISS Index:** تمام نظرات موجود در فایل `data/digikala_samples.csv` را بردارسازی کرده و در یک ایندکس ریاضی ذخیره می‌کند تا جستجو در کسری از ثانیه انجام شود.

#### متد `get_sentiment`:
- متن را به مدل BERT می‌دهد.
- برچسب (Label) را می‌گیرد (مثلاً "5 stars") و عدد آن را جدا می‌کند.

#### متد `retrieve_similar`:
- متن کاربر را به بردار تبدیل می‌کند.
- در ایندکس FAISS می‌گردد و `k` تا از نزدیک‌ترین (شبیه‌ترین) نظرات دیتابیس را پیدا می‌کند.

#### متد `generate_explanation`:
- این بخش **RAG** اصلی است.
- نظرات مشابه را به عنوان "شواهد" (Context) کنار هم می‌گذارد.
- یک **Prompt** (دستور) برای GPT-2 می‌سازد: "این متن کاربر است، اینها هم نظرات مشابه هستند، حالا دلیل فنی این احساس را بگو."
- مدل GPT-2 متن جدیدی تولید می‌کند که توضیح می‌دهد چرا نظر مثبت یا منفی است.

---

## ۳. تحلیل فایل `app.py` (رابط کاربری)
- از **Streamlit** برای ساخت داشبورد استفاده شده است.
- **CSS سفارشی:** برای راست‌چین کردن (RTL) متون فارسی و زیباسازی باکس‌های نتیجه.
- **Tabs:** برنامه به دو بخش "تحلیل" و "آمار" تقسیم شده است.
- **Plotly:** برای رسم نمودار توزیع طول نظرات استفاده شده است.

---

## ۴. نحوه ارائه ویدئویی (پیشنهاد)
۱. **مقدمه:** بگویید که این یک سیستم "تحلیل احساسات با قابلیت استدلال" است.
۲. **بخش RAG:** توضیح دهید که چرا RAG مهم است (چون مدل‌های معمولی فقط یک عدد می‌دهند ولی این مدل شواهد ارائه می‌دهد).
۳. **دمو:** یک نظر وارد کنید (مثلاً: "عالی بود ولی دیر رسید") و نشان دهید که سیستم هم بخش مثبت (عالی بودن) و هم بخش منفی (تاخیر) را درک می‌کند.
۴. **بخش فنی:** فایل `processor.py` را نشان دهید و بگویید از FAISS برای سرعت بالا استفاده کرده‌اید.

---

## ۵. چرخه خودکار (CI/CD)
این پروژه مجهز به **GitHub Actions** است. هر بار که شما کد را Push کنید:
1.  تست‌ها به صورت خودکار اجرا می‌شوند.
2.  اگر تست‌ها پاس شدند، کد مستقیماً به **Hugging Face Spaces** ارسال می‌شود.
3.  برنامه شما در آدرس Hugging Face همیشه به‌روز خواهد بود.

---
**نکته برای یادگیری:** برای یاد گرفتن کدها، سعی کنید مقدار `k` را در `retrieve_similar` تغییر دهید یا مدل‌های دیگری را از Hugging Face جایگزین کنید.
