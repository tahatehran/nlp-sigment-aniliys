# راهنمای ارائه و مستندات پروژه تحلیل احساسات (NLP)

این پروژه به عنوان یک نمونه عملیاتی برای درس پردازش زبان طبیعی در مقطع ارشد هوش مصنوعی طراحی شده است. در ادامه جزئیات فنی جهت ارائه در کلاس آورده شده است.

## ۱. چرخه حیات داده (Data Pipeline)
ما از سه دیتاست معتبر دیجی‌کالا استفاده کردیم. فرآیند شامل مراحل زیر است:
- **تحلیل آنلاین:** داده‌ها مستقیماً از Hugging Face فراخوانی می‌شوند.
- **پاکسازی:** حذف کاراکترهای غیرضروری و نرمال‌سازی متون فارسی.
- **نمونه‌برداری:** انتخاب ۵۰۰ داده برتر برای بهینه‌سازی سرعت در سیستم‌های با رم پایین.

## ۲. معماری RAG (Retrieval-Augmented Generation)
بخش نوآورانه پروژه، استفاده از RAG است. وقتی کاربر نظری را وارد می‌کند:
1.  **بردارسازی (Embedding):** متن کاربر توسط `MiniLM-L12` به یک بردار ریاضی تبدیل می‌شود.
2.  **جستجوی شباهت (Retrieval):** در پایگاه داده FAISS، نظرات مشابه پیدا می‌شوند.
3.  **تولید پاسخ (Generation):** مدل GPT-2 با دیدن نظرات مشابه، دلیل "چرا" بودن یک احساس را توضیح می‌دهد.

## ۳. مدل‌های استفاده شده
- **Sentiment Model:** یک مدل BERT چندزبانه که روی میلیون‌ها نظر آموزش دیده است.
- **Embedding Model:** مدلی سبک و سریع که از زبان فارسی به خوبی پشتیبانی می‌کند.
- **Reasoning Model:** نسخه فارسی GPT-2 که برای درک ساختار جملات نظرات دیجی‌کالا بهینه شده است.

## ۴. داشبورد و UI
رابط کاربری با Streamlit پیاده‌سازی شده که شامل:
- بخش تحلیل زنده با نمایش استدلال هوش مصنوعی.
- بخش آمار و ارقام برای نمایش توزیع داده‌ها.

## ۵. نکات کلیدی برای ارائه
- تاکید کنید که پروژه روی **CPU** و **رم محدود (۸ گیگ)** به راحتی اجرا می‌شود.
- توضیح دهید که مدل‌های مولد ممکن است خطا داشته باشند، اما RAG با ارائه شواهد (نظرات مشابه) این خطا را کاهش می‌دهد.
