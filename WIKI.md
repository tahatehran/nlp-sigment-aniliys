# مستندات فنی و راهنمای خط‌به‌خط پروژه تحلیل احساسات (RAG)

این مستند برای درک عمیق کدها و ارائه در ویدئو تهیه شده است. در اینجا تمام بخش‌های پروژه به زبان ساده و دقیق توضیح داده شده‌اند.

---

## ۱. فایل `prepare_data.py` (آماده‌سازی دیتابیس)
این فایل وظیفه دارد داده‌های خام را از Hugging Face دانلود کرده و یک فایل CSV تمیز برای بخش RAG بسازد.

- **`clean_text`**: فواصل اضافی و کاراکترهای مزاحم را از متن حذف می‌کند تا مدل با داده تمیز کار کند.
- **`load_dataset`**: از ۳ منبع مختلف (Digikala-Comments, Sentiment-Analysis و غیره) نظرات را می‌گیرد.
- **انتخاب ۸۰۰ داده**: برای اینکه سیستم هم سریع باشد و هم دقیق، از هر منبع حدود ۲۶۷ مورد تصادفی انتخاب می‌شود تا مجموعاً ۸۰۰ نظر در پایگاه داده محلی (`data/digikala_samples.csv`) ذخیره شود.
- **فیلتر طول متن**: نظرات بسیار کوتاه (کمتر از ۲۵ کاراکتر) حذف می‌شوند چون برای استدلال مناسب نیستند.

---

## ۲. فایل `processor.py` (قلب تپنده پروژه)
کلاس `SentimentRAG` تمام منطق هوش مصنوعی را مدیریت می‌کند.

### متد `__init__` (راه اندازی):
- **Singleton Pattern**: از الگوی سینگلتون استفاده شده تا مدل‌های سنگین فقط یک‌بار لود شوند و حافظه اشغال نشود.
- **BERT Model**: برای تشخیص مثبت یا منفی بودن (امتیاز ۱ تا ۵).
- **SentenceTransformer**: برای تبدیل جملات به "بردار" (عدد). این کار برای جستجوی شباهت معنایی لازم است.
- **FAISS Index**: یک دیتابیس بسیار سریع برای جستجوی برداری. ۸۰۰ نظر ما را در حافظه نگه می‌دارد تا در کسری از ثانیه شبیه‌ترین نظرات را پیدا کند.

### متد `get_sentiment`:
- متن شما را می‌گیرد و به مدل BERT می‌دهد تا بفهمد چند ستاره است.

### متد `retrieve_similar` (بخش Retrieval):
- متن ورودی را به بردار تبدیل کرده و در ایندکس FAISS جستجو می‌کند تا `k` تا از نزدیک‌ترین نظرات موجود در دیتابیس را پیدا کند. این "شواهد" ما برای پاسخ‌دهی هستند.

### متد `generate_explanation` (بخش Generation):
- **RAG Logic**: شواهد پیدا شده را با متن کاربر ترکیب کرده و به مدل GPT-2 فارسی می‌دهد.
- مدل با دیدن نمونه‌های مشابه، یک "دلیل فنی" تولید می‌کند که چرا این نظر مثبت یا منفی است.

---

## ۳. فایل `inspect_datasets.py` (تست سلامت دیتا)
این فایل یک ابزار نظارتی است که در GitHub Actions اجرا می‌شود.
- بررسی می‌کند که آیا دیتاست‌های آنلاین Hugging Face در دسترس هستند یا خیر.
- اگر دیتایی حذف شده باشد یا دسترسی قطع شود، اجازه Push و Build نمی‌دهد تا برنامه با دیتای خراب بالا نیاید.

---

## ۴. فایل `app.py` (رابط کاربری Streamlit)
این بخش ویترین پروژه است.
- **CSS سفارشی**: تمام استایل‌ها برای پشتیبانی از زبان فارسی (RTL) و فونت "وزیر" طراحی شده‌اند.
- **Tabs**: جداسازی بخش "تحلیل" از بخش "آمار" برای تجربه کاربری بهتر.
- **بهینه‌سازی سرعت**: برخلاف نسخه‌های قبل، این فایل دیگر در هنگام لود شدن اقدام به دانلود دیتا نمی‌کند. داده‌ها توسط GitHub Action آماده شده و همراه کد ارسال می‌شوند، لذا برنامه فوراً آماده به کار است.

---

## ۵. فایل `.github/workflows/main.yml` (اتوماسیون)
این فایل تمام کارهای دستی را خودکار می‌کند:
1. ابتدا سلامت دیتاست‌ها را چک می‌کند (`inspect_datasets.py`).
2. داده‌های جدید را تولید می‌کند (`prepare_data.py`).
3. تست‌های واحد (Unit Tests) را اجرا می‌کند.
4. فایل دیتا را به صورت خودکار Commit کرده و به **Hugging Face Spaces** ارسال می‌کند.

---

## ۶. نسخه Google Colab (`colab/nlp_digikala_colab.ipynb`)
برای کسانی که نمی‌خواهند چیزی نصب کنند:
- این نوت‌بوک یک محیط تعاملی مستقیم (Interactive Loop) دارد.
- شما متن را وارد می‌کنید و مدل در همان‌جا خروجی را چاپ می‌کند.
- نیازی به Streamlit یا تونل‌های شبکه ندارد و کاملاً سبک است.

---
**نکته برای ارائه:** در ویدئو تأکید کنید که سیستم شما "فقط عدد نمی‌دهد"، بلکه با استفاده از معماری RAG، برای هر حرفی که می‌زند "دلیل و شاهد" از دیتابیس استخراج می‌کند.
