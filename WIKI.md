# مستندات فنی پروژه تحلیل احساسات دیجی‌کالا (NLP Master's Project)

این پروژه یک سیستم پیشرفته تحلیل احساسات برای نظرات فارسی (دیجی‌کالا) است که با ترکیب سه تکنولوژی اصلی حوزه NLP پیاده‌سازی شده است.

## ۱. معماری سیستم (System Architecture)

سیستم از معماری **RAG (Retrieval-Augmented Generation)** بهره می‌برد. برخلاف مدل‌های کلاسیک که فقط یک برچسب (مثبت/منفی) می‌دهند، این سیستم دلیل تحلیل خود را با نگاه به داده‌های مشابه توضیح می‌دهد.

### اجزای مدل:
1.  **Sentiment Analysis:** از مدل `nlptown/bert-base-multilingual-uncased-sentiment` استفاده شده که قادر به تشخیص احساس در ۵ سطح (۱ تا ۵ ستاره) است.
2.  **Embedding & Retrieval:** از مدل `paraphrase-multilingual-MiniLM-L12-v2` برای تبدیل متن به بردار استفاده شده است. این بردارها در یک پایگاه داده برداری سریع (FAISS) ذخیره می‌شوند.
3.  **Generative Reasoning:** مدل `HooshvareLab/gpt2-fa-comment` وظیفه تولید متن توضیحی را بر عهده دارد. این مدل با دریافت متن کاربر و نظرات مشابه بازیابی شده، توضیح می‌دهد که چرا چنین احساسی برداشت شده است.

## ۲. مجموعه داده (Dataset)
داده‌ها از سه منبع معتبر Hugging Face تجمیع شده‌اند:
- `fibonacciai/Digikala-Comments`
- `ParsiAI/digikala-sentiment-analysis`
- `EhsanShahbazi/digikala-comments`

برای رعایت محدودیت‌های سخت‌افزاری (رم ۸ گیگابایت)، ۵۰۰ نمونه برتر و با کیفیت انتخاب و در سیستم RAG ایندکس شده‌اند.

## ۳. نحوه اجرا
1.  نصب پیش‌نیازها: `pip install -r requirements.txt`
2.  اجرای برنامه: `streamlit run app.py`

## ۴. نکات مخصوص ارائه (Master's Defense Tips)
- **چرا RAG؟** چون مدل‌های مولد (مثل GPT-2) ممکن است دچار توهم (Hallucination) شوند. با بازیابی نظرات واقعی از دیتاست، استدلال مدل مستند به داده‌های واقعی می‌شود.
- **چرا mBERT؟** مدل Multilingual BERT بهترین تعادل را بین دقت و سرعت در زبان‌های غیر انگلیسی (مثل فارسی) دارد.
- **بهینه‌سازی حافظه:** استفاده از FAISS IndexFlatL2 باعث می‌شود جستجو در بین هزاران رکورد در کمتر از چند میلی‌ثانیه انجام شود.

## ۵. استقرار (Deployment)
پروژه برای استقرار در **Hugging Face Spaces** بهینه‌سازی شده و از **GitHub Actions** برای بروزرسانی خودکار استفاده می‌کند.
