# مستندات جامع آموزشی و فنی پروژه تحلیل هوشمند نظرات (Sentiment-RAG)

این مستند به عنوان یک راهنمای کامل برای یادگیری عمیق، ارائه کلاسی و درک جزئیات فنی پروژه توسط **طه تهرانی نسب** تهیه شده است. در ادامه، تمامی اجزای سیستم به صورت "کلمه به کلمه" و با جزئیات کامل تشریح می‌شود.

---

## ۱. فلسفه و معماری سیستم (High-Level Architecture)

این پروژه از معماری مدرن **RAG (Retrieval-Augmented Generation)** یا "تولید تقویت‌شده با بازیابی" استفاده می‌کند.

### چرا RAG؟
در مدل‌های سنتی تحلیل احساسات، خروجی سیستم فقط یک عدد یا برچسب (مثبت/منفی) است. اما در این سیستم:
1.  **تشخیص احساس (Sentiment):** ابتدا مدل mBERT شدت رضایت کاربر را از ۱ تا ۵ ستاره تخمین می‌زند.
2.  **بازیابی شواهد (Retrieval):** سیستم در پایگاه داده نظرات تاریخی می‌گردد تا نظراتی که از نظر معنایی (Semantic) به نظر فعلی نزدیک هستند را پیدا کند.
3.  **استدلال هوشمند (Generation):** با استفاده از نظرات بازیابی شده به عنوان "زمینه یا Context"، مدل GPT-2 فارسی یک دلیل منطقی تولید می‌کند که چرا این نظر چنین امتیازی گرفته است.

---

## ۲. کالبدشکافی کدها (Code Deep Dive)

### الف) فایل `processor.py` (موتور هوش مصنوعی)
این فایل شامل کلاس `SentimentRAG` است که به صورت **Singleton** طراحی شده تا مدل‌ها فقط یک بار لود شوند.

*   **مدل بردارساز (Embedding):** از `paraphrase-multilingual-MiniLM-L12-v2` استفاده شده که جملات فارسی را به بردار ۳۸۴ بعدی تبدیل می‌کند. این بردارها "معنای" جمله را در فضای ریاضی نشان می‌دهند.
*   **پایگاه داده برداری (FAISS):** این کتابخانه اجازه می‌دهد در بین هزاران نظر، شبیه‌ترین‌ها را در کسری از میلی‌ثانیه پیدا کنیم (جستجوی نزدیک‌ترین همسایه).
*   **مدل تحلیلگر (BERT):** مدل `nlptown/bert-base-multilingual-uncased-sentiment` که روی داده‌های چندزبانه آموزش دیده، مسئول تعیین امتیاز ۱ تا ۵ است.
*   **مدل مولد (GPT-2):** مدل `HooshvareLab/gpt2-fa-comment` که برای کامنت‌های فارسی بهینه شده، وظیفه تولید متن استدلالی را بر عهده دارد.

### ب) فایل `app.py` (رابط کاربری حرفه‌ای)
رابط کاربری با **Streamlit** و با تمرکز بر سادگی و رسمیت طراحی شده است:
*   **پاکسازی بصری:** تمامی آیکون‌های اضافی و شکلک‌ها حذف شده‌اند تا ظاهر برنامه "بیزینسی" و رسمی باشد.
*   **پشتیبانی RTL:** با استفاده از CSS سفارشی و فونت **Vazirmatn**، تمام متون و جداول به صورت راست‌چین نمایش داده می‌شوند.
*   **آپدیت خودکار:** در هر بار اجرای برنامه، اسکریپت `prepare_data.py` فراخوانی می‌شود تا دیتاسِت همیشه بر اساس آخرین داده‌های Hugging Face بروز باشد.

---

## ۳. پوشه اختصاصی Colab (اجرای ابری)

پروژه دارای یک پوشه اختصاصی به نام `colab` است که شامل دو فایل کلیدی می‌باشد:

1.  **`nlp_digikala_colab.ipynb`:** این نوت‌بوک برای اجرای **کامل** پروژه (شامل رابط کاربری) در گوگل کولب طراحی شده است. با استفاده از `localtunnel` یک لینک عمومی به شما می‌دهد تا UI را در مرورگر ببینید.
2.  **`Digikala_NLP_RAG.ipynb`:** یک نوت‌بوک پژوهشی (Research-focused) است که مراحل بارگذاری مدل‌ها و تست‌های مرحله‌ای را بدون نیاز به رابط کاربری نمایش می‌دهد. برای یادگیری گام‌به‌گام منطق RAG، این فایل بهترین گزینه است.

---

## ۴. چرخه خودکار (CI/CD)

فایل `.github/workflows/main.yml` مدیریت کل پروژه را بر عهده دارد:
*   **مرحله اول (QA):** اجرای تست‌های واحد و آماده‌سازی داده‌ها.
*   **مرحله دوم (Deploy):** ارسال خودکار کدها به **Hugging Face Spaces**.
*   **مرحله سوم (Cleanup):** حذف فایل‌های `.log` و پوشه‌های `__pycache__` برای حفظ سلامت و تمیزی مخزن (Repository).

---

## ۵. راهنمای ارائه (Video Presentation Guide)

اگر قصد دارید پروژه‌ را ارائه دهید، این سناریو را دنبال کنید:
1.  **بخش داشبورد:** ابتدا تعداد نظرات مرجع (مثلاً ۵۰۰ عدد) و توزیع طول نظرات را نشان دهید.
2.  **تست زنده:** یک نظر چالش‌برانگیز وارد کنید (مثلاً: "ظاهرش خوبه ولی اصلا کار نمیکنه").
3.  **تحلیل خروجی:** نشان دهید که سیستم چطور هم جنبه مثبت (ظاهر) و هم منفی (عملکرد) را در امتیاز نهایی لحاظ کرده و در بخش "دلیل و استدلال"، شواهد مشابه را از دیتابیس استخراج کرده است.
4.  **بخش فنی:** به سرعت فایل `WIKI.md` و نوت‌بوک‌های Colab را نشان دهید تا عمق کار مشخص شود.

---
**توسعه‌دهنده:** طه تهرانی نسب (Taha Tehrani Nasab)
